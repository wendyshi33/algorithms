reverse String

function reverseString(s) {
  var reverseS= '';
  for (var i = s.length-1; i >= 0; i-- ) 
    reverseS = reverseS + s[i];
  return reverseS;
  }
  
  
  
function reverseString(s) {
  return s.split('').reverse().join('');
  }
